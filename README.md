# Fetcher

## How to install

Clone project & run 
```npm i```

To use dev build run
```npm start```

To use prod build run
```npm run build```

Also don't forget to add your own Google API key (src/sources/youtube/youtubeSH.js) to use search